{
  "name": "example",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "ts-node-dev --no-notify --respawn --transpileOnly ./src/index.ts",
    "install": "yarn generate",
    "generate": "prisma2 generate && NEXUS_PRISMA_DEBUG=true yarn node ../dist/index.js || echo \"\""
  },
  "dependencies": {
    "@prisma/nexus": "^0.0.1",
    "graphql": "^14.3.1",
    "graphql-yoga": "^1.18.0",
    "nexus": "^0.12.0-beta.6"
  },
  "devDependencies": {
    "prisma2": "^0.0.90",
    "ts-node": "^8.3.0",
    "ts-node-dev": "^1.0.0-pre.40"
  }
}
