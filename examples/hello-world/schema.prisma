generator photonjs {
  provider = "photonjs"
}

model User {
  id    Int    @id @unique
  email String @unique
  // handle String
  // posts  Post[]
}

model Post {
  id     Int    @id
  author User[]
}
