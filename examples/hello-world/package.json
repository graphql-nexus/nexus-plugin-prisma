{
  "name": "hello-world",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "generate": "yarn -s prisma2 generate",
    "start": "ts-node --transpile-only app",
    "postinstall": "yarn -s generate"
  },
  "dependencies": {
    "graphql-yoga": "^1.18.3",
    "nexus": "^0.12.0-beta.14",
    "nexus-prisma": "^0.5.0-next.6"
  },
  "devDependencies": {
    "prisma2": "^2.0.0-alpha.243",
    "ts-node": "^8.4.1",
    "typescript": "^3.6.4"
  }
}
