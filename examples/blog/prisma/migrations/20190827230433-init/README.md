# Migration `20190827230433-init`

This migration has been generated by Jason Kuhrt at 8/27/2019, 11:04:33 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."Blog"("id" INTEGER NOT NULL DEFAULT 0 ,"createdAt" DATE NOT NULL  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"name" TEXT NOT NULL DEFAULT '' ,"viewCount" INTEGER NOT NULL DEFAULT 0 ,PRIMARY KEY ("id"))
;

CREATE TABLE "lift"."Author"("id" INTEGER NOT NULL DEFAULT 0 ,"name" TEXT   ,"blog" INTEGER   REFERENCES "Blog"(id) ON DELETE SET NULL,PRIMARY KEY ("id"))
;

CREATE TABLE "lift"."Post"("id" INTEGER NOT NULL DEFAULT 0 ,"title" TEXT NOT NULL DEFAULT '' ,"blog" INTEGER   REFERENCES "Blog"(id) ON DELETE SET NULL,"author" INTEGER   REFERENCES "Author"(id) ON DELETE SET NULL,PRIMARY KEY ("id"))
;

CREATE TABLE "lift"."Post_tags"("nodeId" INTEGER   REFERENCES "Post"(id) ON DELETE CASCADE,"position" INTEGER NOT NULL  ,"value" TEXT NOT NULL  ,PRIMARY KEY ("nodeId","position"))
;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190827230433-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,38 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+}
+
+generator nexus_prisma {
+  provider = "nexus-prisma"
+  output   = "node_modules/@generated/nexus-prisma"
+}
+
+generator photon {
+  provider = "photonjs"
+  output   = "node_modules/@generated/photon"
+}
+
+model Blog {
+  id        Int      @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  name      String
+  viewCount Int
+  posts     Post[]
+  authors   Author[]
+}
+
+model Author {
+  id    Int     @id
+  name  String?
+  posts Post[]
+  blog  Blog
+}
+
+model Post {
+  id    Int      @id
+  title String
+  tags  String[]
+  blog  Blog
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190827230433-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190827230433-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
