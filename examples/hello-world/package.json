{
  "name": "hello-world",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "generate": "yarn -s prisma2 generate",
    "start": "ts-node --transpile-only app",
    "postinstall": "yarn -s generate"
  },
  "dependencies": {
    "graphql-yoga": "^1.18.3",
    "nexus": "^0.12.0-rc.4",
    "nexus-prisma": "^0.5.0"
  },
  "devDependencies": {
    "@types/ws": "^6.0.3",
    "prisma2": "^2.0.0-preview016",
    "ts-node": "^8.4.1",
    "typescript": "^3.7.2"
  }
}
